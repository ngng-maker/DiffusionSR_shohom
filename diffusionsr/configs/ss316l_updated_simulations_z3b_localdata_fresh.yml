root_folder: "./data/expanded_ss316l_all_laser_velocity_xz_cross_section_data_expanded_frame"
downscale_method: "direct"
n_steps: "1"
encoding: True
residual_flag: false
conditioning: "implicit"

normalize_method: "standardize"
schedule: "linear"
fields: 'temperature'
timesteps: 100
batch_size: 32
epochs: 300
learning_rate: 5e-5
use_pretrained: False

# Fresh run on the local diffusionsr/data dataset.
# No restart_dir or encoder_results_dir on purpose.