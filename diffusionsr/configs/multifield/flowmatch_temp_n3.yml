# root_folder is hardcoded to the TRACE cluster path — update for a different machine
root_folder: "/trace/group/forgelab/ngng/multifield/data_fields"
downscale_method: "direct"
n_steps: "3"
encoding: True
residual_flag: false
conditioning: "implicit"
normalize_method: "standardize"
schedule: "linear"
fields: "temperature"
timesteps: 1000
fm_timescale: 1000
fm_n_steps: 50
batch_size: 8
epochs: 300
learning_rate: 5e-5
use_pretrained: False
loss_type: "l1"
