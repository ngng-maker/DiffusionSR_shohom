# FlowMatching + NO encoder (unconditional) + temperature & sdfliqlabel fields
# Ablation 2026-09-01: FM vs LDM  x  enc vs noenc  x  sdf vs temp
root_folder: "/trace/group/forgelab/ngng/multifield/data_fields"
downscale_method: "direct"
n_steps: "3"
encoding: False
residual_flag: false
conditioning: "none"
normalize_method: "standardize"
schedule: "linear"
fields: "temperature_sdfliqlabel"
timesteps: 1000
fm_timescale: 1000
fm_n_steps: 50
batch_size: 8
epochs: 300
learning_rate: 5e-5
use_pretrained: False
loss_type: "huber"
